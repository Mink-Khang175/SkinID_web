# =====================================================
# SkinID — Environment Variables Template
# =====================================================
# SAO CHÉP file này thành .env và điền giá trị thật.
# File .env được gitignore — KHÔNG commit file .env lên GitHub.
# File này (.env.example) là an toàn để commit vì chứa tên biến, không có giá trị thật.

# ----- CLOUDFLARE WORKER SECRETS -----
# Không đặt các giá trị thật vào file frontend hoặc commit lên GitHub.
# Production: dùng `npx wrangler secret put TEN_BIEN`.
# Local: tạo `.dev.vars` (đã gitignore) với cùng các tên biến.

# API key từ Google AI Studio: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# ----- FIREBASE -----
# Lấy từ Firebase Console → Project Settings → General → Your apps
FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789
FIREBASE_APP_ID=1:123456789:web:abcdef123456

# ----- FIREBASE ADMIN (chỉ dùng Cloudflare Worker và scripts quản trị) -----
# Lấy từ Firebase Console → Project Settings → Service accounts → Generate new private key
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-example@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
ALLOWED_ORIGINS=https://skinid-web.your-subdomain.workers.dev

# Dùng riêng cho scripts/import-products-to-firestore.mjs và set-admin-claim.mjs.
FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}
